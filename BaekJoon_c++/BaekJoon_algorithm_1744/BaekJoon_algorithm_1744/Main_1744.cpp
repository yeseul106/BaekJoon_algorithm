#include <iostream>
#include <algorithm>
#include <cstdio>
#pragma warning (disable:4996)

using namespace std;

int main() {
	int N;
	scanf("%d", &N);

	int arr[10000];
	for (int i = 0; i < N; i++) {
		int tmp;
		scanf("%d", &tmp);
		arr[i] = tmp;
	}
	sort(arr[0],arr[N-1]+1);

	for (int i = N-1; i >0; i++) {
		if(a)
		arr[i]
	}
}